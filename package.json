{
  "name": "@kfarkasHU/atfw",
  "version": "0.1.0",
  "bin": {
    "atfw": "dist/src/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "version:commit": "commit-and-tag-version --skip.tag",
    "prepare": "npm run build",
    "test:jest": "jest --config jest.config.ts",
    "test:vitest": "vitest --config vitest.config.ts run"
  },
  "devDependencies": {
    "commit-and-tag-version": "^12.6.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.0.1",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.1",
    "tsx": "^4.22.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "volta": {
    "node": "24.12.0",
    "npm": "11.17.0"
  },
  "dependencies": {
    "ts-morph": "^28.0.0"
  }
}
